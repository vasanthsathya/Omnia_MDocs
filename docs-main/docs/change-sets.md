# Change Sets

A **Changeset** is a logical grouping of pending changes that are applied to the system as a single change.  Making changes to provisioning objects within a changeset ensures that multiple changes are compatible and consistent with the desired network state.

## Related Information
- [Verity API](api.md)