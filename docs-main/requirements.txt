mkdocs-material
mike
mkdocs-glightbox
mkdocs-macros-plugin
mkdocs-redirects
mkdocs-with-pdf==0.9.3
mkdocs-swagger-ui-tag

# Pin the PDF rendering stack. weasyprint is a transitive dependency of
# mkdocs-with-pdf and was previously unpinned, so CI installed the latest
# release. weasyprint >= 63 regressed PDF pagination and silently truncates
# the generated PDF mid-document (verified: 69.0 stops at ~88 pages, 62.3
# renders the full ~362). pydyf is pinned to the version weasyprint 62.3 expects.
weasyprint==69.0
pydyf==0.10.0
mkdocs-unused-files